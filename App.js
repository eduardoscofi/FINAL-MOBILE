import React from 'react';
// Importa o componente que define a navegação da aplicação
import AppNavigator from './AppNavigator'; 

// Componente principal que inicializa a navegação do aplicativo
const App = () => {
  return <AppNavigator />; // Retorna o componente AppNavigator que contém a estrutura de navegação da aplicação
};

export default App;
